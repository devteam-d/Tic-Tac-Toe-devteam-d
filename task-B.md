１task-Bの全体の構造と役割

初期化:
必要なHTML要素を取得し、ゲーム盤と状態をリセットする。
各セルにクリックやマウスオーバーのイベントリスナーを設定。

プレイヤーの操作:
プレイヤーがセルをクリックしたときの処理。選択を記録し、勝敗を判定。

コンピューターの操作:
ランダムに空いているセルを選択して駒を置き、勝敗を判定。

勝敗または引き分けの判定:
勝利パターンとゲーム盤の状態をチェック。



２参考JavaScript用語集

document.querySelector()：htmlファイルの文書で最初の()内の要素を取得する。cssと同じでclassの場合(".")、idの場合("#")で記述する

document.querySelectorAll（）：htmlファイルの文書の()内の要素をすべて取得する。

let : 変数宣言で再宣言不可、再代入可

Array()：（）内に数字を記入するとその数の配列が生成される。数字以外だとその値が配列の一つの要素として格納される

function：関数

forEach:配列の各要素に対して一括で処理を行うために使用。

textContent：テキストの取得、変更を行う

onclick：HTML要素に対してクリックイベントが発生したときに指定した関数を実行させるために使用

addEventListener：指令されたイベントが発生したときに実行する処理を登録するために使われるメソッド

mouseover：マウスカーソルが指定した要素の上にのったときに発生する

mouseout：マウスカーソルが指定した要素から外れたときに発生する

map()：配列の各要素に対して処理を行い配列を返す

Math.random()：０以上1未満のランダムな浮動小数点を返す

includes()：配列に（）内の要素があるか調べる。存在する場合true、存在しない場合falseを返す